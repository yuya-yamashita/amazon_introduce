<h1>Sites#index</h1>
<p>Find me in app/views/sites/index.html.erb</p>
<%= link_to('新規サイト作成', new_site_path)%>
<% @sites.each do |site| %>
	<div>
		サイト名：<%= site.name %>
		紹介：<%= site.description %>
		<%= link_to '詳細', site_path(site.id), method: :get %>  編集 削除
	</div>
<% end %>
